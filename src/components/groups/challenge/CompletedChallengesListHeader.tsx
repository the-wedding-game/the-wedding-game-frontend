import { Title } from "@mantine/core";

export default function CompletedChallengesListHeader() {
    return (
        <div>
            <Title order={1} className={`text-gray-700`}>
                Completed Challenges
            </Title>
        </div>
    );
}
